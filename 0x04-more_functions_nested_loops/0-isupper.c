#include "main.h"

/**
*main - _isupper fucntion to check
*for uppercase
*return: (1) if uppercase or (0) if not
*/

int _isupper(int c)
{
	for (c= 'A'; c<= 'Z'; c++)
	{
	return (1);
	}
return (0);
}
